{% extends 'base.html.twig' %}

{% block title %}CategoryArt{% endblock %}

{% block body %}
    <h1>CategoryArt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_art.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_art.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ category_art.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_art_index') }}">back to list</a>

    <a href="{{ path('category_art_edit', {'id': category_art.id}) }}">edit</a>

    {{ include('category_art/_delete_form.html.twig') }}
{% endblock %}
